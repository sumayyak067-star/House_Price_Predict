/** @deprecated Use `@/data` — kept for backward-compatible imports */
export {
  distributionData,
  scatterData,
  bedroomDistribution,
  featureImportance,
  priceByCityData,
  actualVsPredicted,
  correlationData,
  cities,
  performanceData,
  metrics,
  datasetStats,
} from "./index";
